# Policy, Authority and Access Controls

These files are intended to be **deterministic inputs to the architecture**. They are not prompt suggestions.

Participants should decide where enforcement belongs (IAM, API gateway, retrieval filters, policy engine, workflow state machine, database row-level controls, etc.), but the invariants themselves must survive the design.
