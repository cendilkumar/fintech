# Engineering Tasks
Tasks are implementation units derived from approved specs, not substitutes for specs.

Lifecycle: `backlog → ready → active → verification → completed`.

Every task must list requirement IDs, acceptance criteria, domain guardrails, affected files, tests/checks and evidence output.
